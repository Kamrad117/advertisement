<div class="row">
  <% @banners.each do |banner| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag banner.image.url(:medium), class: 'image'), banner %>
      </div>
      <hr/>
    </div>
  <% end %>
</div>
